**Creation** 2026-07-08 — Initial OKF bundle generated from `ARCHITECTURE.md` (Agents SEO system). Covers 22 concept files across 6 domains: components (9), agents (4), data contracts (3), scoring (2), external dependencies (2), and architecture decisions (4). All concepts cross-linked; index files generated by `okf_tools.py`.

**Update** 2026-07-08 — Synced the bundle to the implemented codebase. Added a `status` extension field to every concept (`implemented` / `partial` / `scaffold` / `contract` / `planned`) so the bundle reflects build reality. Corrected `components/cache` (SQLite → Redis, as built) and rewrote `components/api` (implemented `POST /api/sitefacts` vs planned audit/SSE/PDF routes). Added implementation-status notes to `crawl-fetch`, `extraction`, `data/site-facts`, `data/agent-result`, and `model-router`, recording the deviations from the spec's ideal libraries (BeautifulSoup JSON-LD not `extruct`; heuristic entities not `spaCy`). Added two concepts: `components/pipeline` (the implemented URL→SiteFacts path) and `decisions/scope-sitefacts-first`. Indexes regenerated; bundle re-validated.

**Update** 2026-07-08 — Full agents + integration layer built and reflected. Key changes: (1) All four agents (`crawlability`, `content_signal`, `structured_data`, `entity_topic`) and the aggregator promoted from `scaffold`/`planned` to `implemented`. (2) Crawlability agent rewritten to document the 3-pass deterministic httpx+Tranco sub-agent (no Firecrawl). (3) Scoring weights updated per Google AI Optimization Guide (`content_signal` 35%, `structured_data` 15%); commodity content cap documented. (4) Visibility estimate updated with latency signal, stricter JS penalties, llms.txt scoped to Claude/Perplexity only. (5) Model router updated to dual-backend design (Ollama local + vLLM remote via cloudflared tunnel). (6) `components/vllm-server` updated from local Docker/ROCm to Jupyter-hosted tunnel approach. (7) `components/api` updated: `POST /api/audit` is now implemented as an integration bridge (SiteFacts pipeline → agents-api via `AGENTS_URL`). (8) Added new concept: `decisions/google-aio-alignment` documenting the scoring realignment. Bundle now 28 concept files. Indexes regenerated; validated CONFORMANT.

**Update** 2026-07-08 — Frontend built (`frontend/`, status `partial`): story hero, SiteFacts strip, four skeleton-loading agent columns wired to the future `GET /agent/stream/{agent_id}` SSE contract (graceful offline state until it ships), and a report file-chip → split pane with .md/PDF export. anime.js drives transitions. `components/frontend.md` rewritten to record the amended design and its deviations from the original spec.

**Update** 2026-07-08 — SSE agent-streaming feature implemented and documented. Key changes: (1) Added `agents/app/state.py` — in-process asyncio.Queue registry (one queue per agent_id, one result dict per audit_id). (2) Added `AgentStatusEvent` and `AuditStartRequest` schemas to `agents/app/schemas.py`. (3) All agents extended to emit phase events: `BaseAgent.run()` gains optional `agent_id` param and emits `started → building_prompt → llm_call → [retry] → parsing_result → complete/error`. Crawlability agent emits additional `sub_agent_pass_1/2/3`, `judgment_call`, `applying_hard_gates`. (4) Three new routes on agents-api: `POST /audit/start` (fire-and-forget), `GET /agent/stream/{agent_id}` (SSE), `GET /audit/{audit_id}/result` (poll). (5) Three new proxy routes on backend: `POST /api/audit/start`, `GET /agent/stream/{agent_id}`, `GET /api/audit/{audit_id}`. (6) Comprehensive test suite added at `agents/tests/` (30 tests across 4 files: state, schemas, streaming endpoints, agent emission). (7) New scripts: `agents/dry_run_streaming.py` (live SSE terminal view), `agents/frontend_mock.py` (full frontend flow simulation). OKF updated: `components/api.md` promoted to `implemented`; new concepts `components/streaming.md` and `data/agent-status-event.md` added. Bundle now 30 concept files. Indexes regenerated; validated CONFORMANT.
